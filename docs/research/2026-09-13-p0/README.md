# Mem0/Qdrant P0 实测记录

本页归档 Mem0/Qdrant 原型的实测，后台存储设计见[原型说明](mem0-architecture.md)。报告固定为该次运行；该路线现已退出，原型代码和运行依赖已移除。JSON 中的路径、命令与代码 hash 仍保留历史值，不代表当前可执行入口。

日期：2026-09-13。隔离原型已从源码运行，默认 Copilot profile 未改动，也未安装正式产品。真实模型对照尚未执行，目前没有经验质量或用户收益结论。

| 检查 | 结果 | 能说明什么 |
|---|---|---|
| TypeScript 与单元测试 | 类型检查通过，23/23 测试通过 | 适用性门槛、来源/修订排除、用户隔离、协议生成及任务判定器的局部行为 |
| Mem0 + Qdrant + 本地嵌入 | 12 项检查通过，本次约 12.5 秒 | 逐条字段、小样本范围检索、精确过滤、metadata 编辑、停用、分页、管理点、删除和确认写后进程重启 |
| 隔离 Copilot 版本/帮助 | CLI 1.0.83 通过 | 二进制与预检选项可用；不代表插件体验 |
| 真实 CLI + 本地脚本响应 | 3 个协议场景观察成立 | 指令回填、明确工具拒绝、超时继续执行；不是模型推理评测 |
| 三组任务对照 | 未执行，缺真实模型配置 | 只有入口与两组手写机制样本，尚无提炼/任务净收益结果 |

## 实测发现

Mem0 3.1.8 发布包会直接导入未使用的 better-sqlite3 和 pg。未安装 SQL 包时，还没构造 Qdrant 配置就会失败。P0 用 loader 对这两个 provider 返回抛错实现，Mem0 和 Qdrant 算法保持原样。检查未发现 SQLite 数据文件，但通过依赖了这层适配；正式发布前仍须解决包加载问题。

Qdrant JS client 1.19.0 移除 Mem0 调用的 search 方法，实际搜索报错；固定到 1.18.0 后通过。Qdrant server 为 1.19.1。本地嵌入使用固定版本 MiniLM q8、384 维，只验证英文小样本，不代表中文质量。

CLI 1.0.83 实际在首次模型请求前应用 userPromptSubmitted.modifiedPrompt 和 userPromptTransformed 的变更。前者与读取到的当前官方说明不同，报告保留了差异；实现须绑定已测版本，不能只按网站说明猜测行为。

preToolUse 明确 deny 阻止了测试目录的 view 读取；hook 超时则继续执行读取，postToolUse 收到实际文件结果，后续脚本模型请求也包含该结果。因而不能靠 hook 超时建立“记忆服务不可用就一定停止工具”的保证。采用前刷新仍须有显式失败处理与真实 Agent 配合验收。

宿主试验使用 loopback 脚本响应，共 5 次请求、0 次真实模型调用，未提供 Copilot 账号凭据。结果只说明观察到的接口时序，Agent 是否正确采用经验、异步纠正回执 UI 是否可用都尚未验证。

## 保存的结果

- [存储与重启](storage-probe.json)
- [宿主预检](host-preflight.json)
- [宿主三场景与官方说明差异](host-hooks.json)
- [真实模型对照的阻塞记录](comparison.json)
- [命令结果与代码 hash](validation.json)

通用宿主验证已迁至[Copilot 探针](../../../probes/copilot/README.md)，适用性与任务判定器已迁至[evals](../../../evals/README.md)。上述历史测试不覆盖写到一半被终止、未知提交自动恢复、长期队列压力、多层真实提炼、统计可靠的迁移收益或完整安装流程。

查看删除前的原型源码可使用 Git 历史；该提交包含旧文件，不表示其全部行为都已在上述运行中验证：

```powershell
git show e4fea3e:spikes/p0/README.md
git show e4fea3e:spikes/p0/mem0-gateway.ts
```

范围测试加入了同文本的错误 scope、停用和过期记录，数量仍小于 Mem0 内部候选上限。因此报告只确认小样本筛选行为，不能算完成“候选限额前过滤”的压力验收；更大干扰集合还需测试。
