# 第一阶段实现计划：P0–P3

日期：2026-09-15。状态：范围已确定，核心与主要入口已有实现，剩余功能和运行验收见本页进度。第一阶段交付 P0–P3 的完整首版；P0、P1、P2、P3 是阶段内的工程关卡，通过 P0 后继续推进，不把技术样例作为本阶段终点。

本页维护复用方式、工作包、依赖、代码落点和进度。[05](05-delivery-and-validation.md)维护验收要求与发布门槛，[07](07-storage-model.md)维护字段、容量和保留期。实现计划不另设一套产品合同。

## 第一阶段交付范围

交付单用户、本机运行的 LessonLoop：从获准工作材料整理案例，实际执行 L1–L5 提炼，形成和更新方法，经方法库 UI/CLI 查看、管理和导出，并在 Copilot CLI 的新任务中准备和使用方法。后台学习、方法自动推荐、效果回顾及通知分别设置。

阶段内包含样例 Connector、效果回顾和 Windows 安装、后台、升级与卸载。“可选回顾”表示用户可选择开启，功能仍须交付。P4 的更多真实来源、其他宿主、第二种 MemoryEngine、多用户及 Skill 自动安装同步不在本阶段范围。

当前没有需要保留的旧用户数据，首版从当前格式初始化。旧开发 bank 布局、旧原型和未发行版本的数据转换不列入待办；保留当前格式的备份恢复、兼容检查及用户控制。方法用于较晚未见任务的迁移能力仍须验收。

核心、学习与方法管理、宿主适配、样例 Connector、效果回顾和私有运行时已有代码。工程测试、真实模型案例和发行验收分别记录，见本页末尾。

## 复用优先的实现方式

官方能力依据见[复用核查](research/2026-09-14-agent-integration-reuse/README.md)。源码核查固定在该记录的提交；W01 另固定实际可运行、可分发的组件版本。每项实现先用官方能力验证相应场景，再补具体缺口。

| 能力 | 首选实现 | LessonLoop 增量 |
|---|---|---|
| 存储与基础 API | 官方文档、facts/observations、来源、bank/tag、索引和 SDK | 保存产品对象、稳定身份、授权、修订及控制；不复制原生图、向量或提取存储 |
| 原生后台处理 | 官方持久作业、查询、取消、重试和清理 | LearningJob 关联原生操作，记录产品阶段、取消意图及发布结果；不另造底层任务执行器 |
| 经验形成 | retain/observations mission 与官方提取、归纳 | 工作案例组织、L1–L5 目标、来源独立性与准入检查 |
| 方法生成和更新 | Reflect 的结构化输出；Mental Model 的 playbook、自动刷新、delta 更新和历史 | 方法 Schema、主题选择、结构与支持检查，将合格结果发布为产品 Playbook 修订 |
| 检索与方法使用 | 官方检索、标签过滤和候选读取 | 当前发布资格、版本与依赖核对，preparePlaybook 的完整指导与反馈关联 |
| UI/CLI | 官方内容、编辑、历史及比较组件与 SDK | 接到产品 API，补方法视图、适用性、任务准备、控制回执及导出 |
| Copilot 接入与模型 | 官方 hooks、会话读取/写回、上下文注入、安装诊断和 Copilot provider | 产品调用转换、真实任务边界、完整方法投递、结果关联与范围检查；先补官方模块接线 |
| 使用记录 | 官方 usage/diag 中含义相符的记录 | 实际投递、采用、结果与方法变化的分别关联，效果回顾和通知 |

官方专用 Copilot 插件和统一 coding-agents 包均已有采集与回填能力，触发时机及覆盖按选定版本核对。页面复用官方适合的管理组件，负责方法操作和任务关联；多轮执行留在 Copilot，不另建页面执行器。

Mental Model 是首选的方法生成与维护载体，ProductStore 保存已通过检查的产品正文、依据引用、版本和用户控制。原生自动刷新产生候选，完成产品检查和投影确认后才生效。直接原生调用只用于隔离基线或维护；复用 UI 组件时也要转换其读写路径。

PublishedProjection 先验证官方独立命名空间和前置过滤能否满足合同；只有场景证明不足时才增加可重建索引。来源或方法停用的产品屏障始终存在。原生来源重处理会重置 curation，Mental Model 的新写入检测也不能替代来源删除后的即时失效处理。

官方配置、输出 Schema、薄适配即可满足的功能，按配置和适配交付。新增引擎代码、长期维护的上游补丁或额外索引，要记录触发它的失败场景、改动范围和对应验收；不按模块名称预先重写官方能力。

## 工程落点

沿用仓库的 TypeScript、Node.js 和 Zod。一个产品核心进程提供带版本的 API；UI、CLI、Agent 和 Connector 使用同一客户端。Hindsight、数据库及本地模型组件仍按[03](03-architecture.md)和[10](10-distribution-and-installation.md)运行。

以下是当前代码落点，构建和类型检查已接入。各目录存在不代表对应工作包已验收。

| 位置 | 内容 |
|---|---|
| `src/domain/` | 04/07 的产品 Schema、引用、资格、方法结构与准备规则 |
| `src/core/`、`src/store/` | 产品 API、服务编排、PostgreSQL 独立 schema 初始化及版本检查、写协调 |
| `src/adapters/hindsight/`、`src/adapters/copilot/` | 官方 SDK/模块封装、能力映射与产品协议转换 |
| `src/cli/`、`src/ui/` | 产品操作入口；UI 优先复用可接到产品 API 的官方组件 |
| `src/connectors/`、`src/core/effects.ts`、`src/core/reviews.ts` | 样例接入、可靠接收、结果路由、回顾与通知 |
| `config/`、`distribution/` | 固定组件清单、学习配置、Windows 构建与安装管理 |
| `tests/`、`evals/` | 合同、集成、宿主、发行测试和真实方法评测 |

现有 `evals/lib/experience.ts` 只迁移仍符合合同的规则和测试。`evals/lib/model-client.ts` 不作为重写 Copilot provider 的起点。官方包及复用源码记录版本、许可和局部改动。

## 工作包与依赖

每个工作包可以拆成多个可运行、可验证的 PR。表中“完成证据”说明本包应提交的产物，是否通过对应关卡仍按 05 判断。各包已有部分实现，当前证据见本页末尾和运行记录。

| 编号 | 工作与产物 | 依赖 | 完成证据与验收归属 |
|---|---|---|---|
| W01 官方运行与复用验证 | 固定引擎、宿主、provider、嵌入/重排和数据库版本；配置 mission、方法 Schema、刷新及保留策略；用真实材料跑出原生方法候选；验证 Windows 组件组合 | 无 | 固定清单、可重复运行入口、真实输出/来源/用量、原生满足与具体缺口记录；P0 |
| W02 最小产品核心 | 产品 Schema、API/客户端、授权配置、独立存储初始化与版本检查、Source/WorkView 接收、LearningJob 与原生 operation 关联、修订与写屏障 | W01 的接口与状态证据 | 可靠接收、幂等、冲突、超时核对、取消和重启恢复；E02/03/05，X01，D/A 相关规则 |
| W03 从案例形成方法 | 整理动作与结果；选择主题；复用原生提取/归纳/结构化综合；校验支持和方法结构，形成产品经验与方法候选 | W01、W02 | 从真实材料生成方法，普通材料正常零产出，来源及处置可追溯；MTH01/02，E01，L/A/D 相关规则 |
| W04 发布、检索与准备 | 同步合格对象投影；实现 search/browse/inspect/recall 与 preparePlaybook；检查权限、当前修订和依赖，返回完整条件、步骤与分支，保留结果关联 | W02、W03 的对象；W01 的索引验证 | 中英/精确检索、失效过滤、完整分支与检查归属、输出预算和任务有效性；MTH04/05，E04/05，R/T/D 相关规则 |
| W05 演进与完整生命周期 | 复用原生 delta/历史；跨案例 L2–L5、更新/拆分/无增量；实现持久纠正、依赖失效、来源保留清理、当前格式导出及恢复 | W03、W04 | 新结果改变具体方法；来源重处理、迟到旧写、重启和恢复不撤销控制；MTH03/06–09，E01–E08，L/C/D/A 相关规则 |
| W06 Copilot 产品接入 | 选定并改造官方采集/回填模块；产品 MCP、可信事件入口、任务注入、显式准备、结果捕获和回执；隔离学习会话 | W01、W02；方法调用依赖 W04，生命周期回归依赖 W05 | 真实宿主工作轨迹、实际注入与结果关联、取消/迟到/失败场景和能力清单；MTH05，E09，U、X02/04 |
| W07 方法库与操作入口 | 先交最小 UI/CLI，再补编辑、历史比较、准备、状态、来源与设置、Markdown/checklist/Skill 导出；复用官方适合的组件 | W02–W04；历史/控制依赖 W05，任务关联依赖 W06 | 同一方法经 UI/CLI 操作结果一致，快照版本与限制完整，纠正回执符合事实；MTH10/11，X05 |
| W08 样例接入与效果回顾 | 交付可复现样例 Connector、Runtime 和管理入口；四个独立设置；真实事件分别路由学习/回顾、统计、周期回顾、通知和开发导出 | W02、W05；使用证据依赖 W06，界面依赖 W07 | 样例覆盖快照、追加、纠正、撤回与重放；回顾保留 unknown 和完整分母，摘要不循环学习；K01–K13、MTH12、O01–O12 |
| W09 Windows 发行与运行 | 从 W01 起验证依赖和私有运行时；实现组件清单、后台管理、安装/配置/诊断、自启、升级/回滚、卸载及清理 | 依赖切片仅需 W01；最终打包依赖 W02–W08 | 干净 Windows 安装并完成方法任务；后台、路径、失败恢复和最终字节校验；I01–I14、X06 |
| W10 全阶段质量与交付验收 | 从 W01 起整理场景与独立判定，补材料和未见任务，冻结官方对照与成本配置；汇总跨模块故障、性能和发布报告 | 逐包接入；最终报告依赖 W01–W09 | 按 05/09 执行方法、组成规则、质量、未见任务使用、收益、性能及发行验收，失败项有定位与复测记录 |

W02 首先实现 W03/W04 所需的最小存储和 API，随后与学习流程一起扩充。W06 的基础自动/显式准备路径和 W07 的最小入口在 P1 完成，全部宿主回归与完整操作在 P3 完成。W08 的设置与事件身份在 W02/W06 提前接好，W09 的 Windows 依赖及 W10 的评测材料从开工就验证。

## 阶段内关卡

| 关卡 | 工作包达到的状态 | 交付后的下一步 |
|---|---|---|
| P0 | W01 完成；W02–W04 有一个主题的最小路径；W06 取得真实采集/回填证据；W09 完成依赖切片；W10 固定场景与运行 profile | 记录未决项并继续实现 P1，后续工作已在第一阶段范围内 |
| P1 | W02–W04 的方法形成与使用可运行；W06 完成新任务自动 search→prepare→回填、显式准备及可信采集；W07 最小 UI/CLI 可用；完成一次真实任务及结果关联 | 扩展跨案例提炼、方法演进和生命周期 |
| P2 | W05 完成，W03 支持实质 L2–L5；W04 经过纠正、依赖、来源与未见任务使用回归 | 完成全部入口、接入、回顾和发行 |
| P3 | W06–W09 完整交付，W10 汇总通过 05/09 要求的全部报告 | 第一阶段完成；P4 真实来源扩展另列后续任务 |

上表用于安排工作，不缩减[05 的退出条件](05-delivery-and-validation.md#实施顺序)。真实宿主接入、全部方法功能、样例 Connector、可选回顾、安装交付和质量对照均不能作为“后补项”留在第一阶段之外。

## 贯穿开发的案例

先使用生成文件与手写文件案例，验证输入、方法形成、准备和结果回流。随后加入不同主题的案例族验证 L3/L5 和迁移；一个生成文件例子不能覆盖全部层次。

1. 记录手工修改生成文件被覆盖、修改权威源后保留的真实尝试，形成 WorkView 与有来源的方法。
2. 新任务的维护来源未知时，准备结果先给诊断步骤；取得观察后，生成文件和手写文件分别选择正确路径。
3. 新材料说明特定版本允许扩展区手工代码，方法新增有限分支，保留原先有效部分；重复成功没有新信息时不改产品版本。
4. 用户纠正、来源撤回、原生重处理及重启后，旧建议不再投递；新版本未确认时明确保持暂停。
5. 在较晚未见任务中使用修订后的方法，查看历史与实际结果，并从最终 Windows 安装包重复完整路径。

学习器只接收获准正文、真实角色和上下文。预期答案、审阅说明、判定标签和未来迁移任务信息不进入提炼；留出学习材料不用于调参，仅在冻结配置后的验收运行中输入。已有材料须经适配与独立判定后才能成为验收场景。

## 实现中的决策与故障处理

| 决策点 | 首选与需要核实的结果 | 不成立时的处理 |
|---|---|---|
| 原生方法生成 | mission、Reflect/Mental Model Schema 和 delta 能表达目标方法，并给出可核对来源 | 先调整配置和 Schema；仍失败才增加方法组装逻辑，记录具体失败场景 |
| 原生作业与发布衔接 | 可靠取得操作身份、阶段、取消及终态，产品发布另行确认 | 保留 uncertain 和屏障，先查原操作；不盲重放或把原生 completed 当产品生效 |
| 发布投影 | 原生命名空间可前置过滤资格，写后读回与产品版本对应 | 增加可重建投影；保留官方学习索引，不重做提取 |
| 原生 UI 复用 | 组件可通过产品 API 读取、修改及显示回执，原生入口隔离 | 只补必要产品页面或组件，不为复用界面开放控制旁路 |
| 原文与长期依据 | 产品和原生副本能按 07 清理，当前方法仍有获准的长期支持 | 暂停不合格方法，调整明确的保留配置或管线；不暗留全文 |
| Windows 默认组合 | 固定数据库扩展、引擎、provider 和本地模型组件可按 10 打包运行 | 继续不依赖该问题的实现，记录阻塞；需要改变产品支持范围时先形成具体可审阅方案 |

日常技术选择在已确定范围内推进。P0 的未验证项用于控制依赖和判断实现方式，不要求每通过一个关卡重新申请开工。产品范围或关键合同若需改变，先同步相应主文档和影响说明。

## 非发行功能的实现与待验收项

本轮在现有核心上补齐产品接入和操作，复用官方模块及已有等价路径。下表区分已落地行为和仍需覆盖的验收；测试通过不代表P0–P3全部通过。

| 工作 | 本轮实现 | 验证与限制 |
|---|---|---|
| W06：Copilot采集、任务边界和方法指导 | 适配固定官方会话读取，捕获真实角色/工具结果/最终回复；去除重复注入及自身MCP派生材料；保留playbookUseRef，处理重放、结束重试与迟到 | 早期真实CLI已验证回填、工具读取和最终回复采集。当前默认一次提供完整指导，不再同步复核分支；活跃任务中的补充保持关联，停止后可用/lessonloop continue继续，/lessonloop new明确新建。会话结束后重新创建任务；不承诺识别任意自然语言换题 |
| W06/W08：真实效果事件 | 宿主回填回执记delivery；真实工具结果供后台复盘，缺少采用依据时保持unknown；结束、失败与采集缺口有明确事件；用户可评价已关联方法 | 历史真实宿主验证覆盖投递、三种来源角色和unknown结果。当前获取与反馈关联保留同一playbookUseRef，取消同步复核派生的usage；采用及实际收益仍按真实任务验收 |
| W07：方法库完整管理 | 范围/主题/状态筛选、分页、常用固定；步骤增删重排、分支/检查编辑、旧版内容送审；关联宿主任务、复制引用及使用评价 | 旧依据变更需明确确认当前引用，缺失或暂停依据会阻止送审；异步响应绑定原任务。DOM事件与HTTP fixture检查通过，真实浏览器视觉验收仍待进行 |
| W02/W04/W07：来源、经验与方法入口 | 对外仅 Source、Experience、Playbook；原文提交与管理合并到来源页，工作记录通过 getWorkView 展开，使用记录通过 getUsageView 生成；sourceFor 补结果保留原任务 | 公共接口、片段身份、宿主归属、连续及乱序补充、取消脱敏均有回归；原始记录与内部缓存保留，不要求用户维护案例 |
| W08：样例Connector合同 | 普通读取/规范化模块支持案例、经验草稿、方法草稿；稳定UTF-8分片，完整清单原子接收，全部作业终结后完成，失败片单独重试 | 单元与数据库验证中断回滚、旧支持不提前撤回、同来源族不重复算支持、父忘记覆盖全部片；远程认证和真实来源仍在P4 |
| W03/W05：学习与自动演进质量 | 审查逐条给出当前执行路径和原方法路径的保留判定，整体批准不能覆盖路径否决；仍最多修正一次，保留全部操作及失败 | 新的干净自动演进生成修订，真实provider对原字段路径和版本2扩展路径的准备检查通过；完整L2–L5和未见任务泛化仍需独立材料验证 |
| W01/W10：检索组件与评测工具 | 固定官方FlashRank本地模型，修正包内不匹配tokenizer；数值计量按操作去重；开发三组执行器复用官方provider、SDK及产品核心，隔离范围/任务/失败报告 | 本地重排4组中英离线预测通过，缺模型明确RRF降级；无法计量的原生步骤保持partial/unknown。开发SDK基线不代表官方Agent hooks对照，完整05/09规模与收益门槛仍待验收 |

代码落点：[Copilot适配](../src/adapters/copilot/hook.ts)、[核心API](../src/core/server.ts)、[服务逻辑](../src/core/service.ts)、[页面](../src/ui/app.js)、[样例Connector](../src/connectors/sample.ts)。官方采集/管理能力依据见[复用核查](research/2026-09-14-agent-integration-reuse/README.md)，自动演进失败与局部验证见[运行记录](research/2026-09-14-implementation/README.md)。

定向补证已更新同一经验的修订并使旧依赖暂停；验证脚本进一步检查发布回执和重新召回，依赖方法不会未经审查自动恢复。来源清理、并发控制、恢复、全部K/O系列等继续按当前格式做回归，不能因没有旧用户数据而取消。

当前不做旧开发布局迁移、页面工作流执行器、更多真实Connector、第二引擎、多用户或Skill自动安装同步。Windows安装、自启、卸载、永久清理和发行故障矩阵仍归W09，另按10/05推进。

## 验证与进度记录

每个 PR 写清所属 W 编号、对应验收 ID、复用组件、实际新增内容、验证命令与结果。新增运行入口时同步 `package.json` 和 README；尚未实现的命令不列为可运行命令。

开发报告写入 `.local-validation/results/`。里程碑证据经脱敏后保留到 `docs/research/`，记录产品提交、组件清单、profile、输入划分、命令、产物、失败与限制。原生/产品作业状态、方法发布状态和效果证据分别报告；旧探针结果不继承为新版本通过。

单包通过相关测试后推进下一包。到 P3 用实际发行字节执行完整验收；具体样本量、质量与性能门槛只引用 05/09，不在本页复制。

| 关卡 | 当前状态 | 通过证据 |
|---|---|---|
| P0 | 实现中，未通过全部退出条件 | 已有真实 Windows 组件、方法生成/准备、中文检索及真实宿主 lead 回填；见[运行记录](research/2026-09-14-implementation/README.md) |
| P1 | 核心、管理入口与宿主接入已有实现，完整验收未完成 | 历史真实CLI回填、读取和最终回复采集通过；当前改为完整方法指导，材料与任务边界故障场景有回归，新默认链路另行验证 |
| P2 | 演进与控制已有实现，扩大质量验证待完成 | 逐路径审查后干净自动演进保留字段/扩展路径；修订/拆分、来源控制与补证有回归。历史失败保留，完整L2–L5与未见任务使用未验收 |
| P3 | 非发行入口已补齐，完整质量与发行验收未完成 | 分片Connector、方法库管理、效果事件和本地重排已有功能验证；真实浏览器视觉验收、完整对照/故障矩阵及最终安装交付仍待进行 |

首批工作从 W01、W02 最小工程入口、W09 依赖切片和 W10 场景准备展开，随后接上 W03/W04 的真实方法路径。工作量按“直接复用、配置适配、产品新增”分别记录；完成 W01 后依据实际缺口估算剩余工作，先前未充分区分复用范围的总人日不作为排期基线。
